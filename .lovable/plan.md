## What I understood from your request

Create **CYC-012 — Teacher Reports — Exams QA** from `teacher-reports-exams-qa.md`, using the same clean-build pattern as CYC-010 and CYC-011. Cycle 13 (Students) will follow when its doc is uploaded.

## Source-document audit

Parsed 811 lines. Confirmed structure:

| Group | Section title | Scenarios |
|---|---|---|
| A | Exams Tab Listing — My Exams vs Institute Tests | A1–A6 (6) |
| B | Institute Test Subject Scoping (HIGHEST PRIORITY) | B1–B8 (8) |
| C | Exam Detail — Overview Sub-tab | C1–C5 (5) |
| D | Questions Sub-tab + Reteaching Plan | D1–D6 (6) |
| E | Chapters Sub-tab | E1–E4 (4) |
| F | Difficulty Sub-tab | F1–F4 (4) |
| G | AI Touchpoints — Actionable Insights & AI Deep-Dive | G1–G8 (8) |
| H | Edge Cases, UI/UX & Stability | H1–H10 (10) |
| I | Institute Test Detail Page Deep-Dive | I1–I9 (9) |

**Total: 9 groups, 60 scenarios.**

The doc preamble (lines 1–46) contains the same four-part structure as CYC-011, scoped to exams:
1. Intro paragraph (subject-scope correctness + AI prefill drift framing).
2. **Threshold Reference table** (75/50/35 canonical vs 65/40/35 UI hard-codes vs Pass/fail bands).
3. **Before You Begin — Seed Your Data First** (45-min seeding checklist with 7 bullets covering teacher exams, institute Grand Tests, multi-batch publishing, sub-50% questions, all-100%/all-0% extremes, absent students).
4. **Highest-Risk Bugs to Hunt** (6 numbered items: P0 subject leakage, prefill drift, AI Deep-Dive contamination, stale verdicts after retest, institute-test visual distinction, lost `returnTo`).

Sections A–I in this doc all use the explicit "What to try / Expected" pattern with a leading description paragraph, so the 3-block normalization is straightforward (no special handling like Group I needed in CYC-011).

## Pre-flight check

- Project: `11111111-1111-1111-1111-111111111111` (foundational).
- Created by: `88326c88-1370-4d30-8eeb-e05941e74931` (admin, matches CYC-001 through CYC-011).
- Next cycle code is auto-generated by trigger `generate_cycle_code` → will produce `CYC-012`.
- ID convention: matching CYC-008/009/010/011 → `e1f2a3b4-c5d6-4e7f-8a90-111111111112`.
- No bug/comment/verdict data exists for CYC-012 → clean build, no preservation needed.

## Implementation plan

### Phase 1 — Insert the cycle row
Single insert into `test_cycles`:
- `id = e1f2a3b4-c5d6-4e7f-8a90-111111111112`
- `name = 'Teacher Reports — Exams QA'`
- `status = 'active'`, `priority = 'medium'`
- `project_id`, `created_by` as above
- `description` = ~5–6 KB markdown built from the doc preamble: opening paragraph + Threshold Reference table + Before You Begin checklist + Highest-Risk Bugs list. Trigger fills `cycle_code = 'CYC-012'` automatically.

### Phase 2 — Parse the markdown
Reuse the CYC-011 Python parser approach:
- Split by `### ` group headers (A–I).
- Within each group, split by `**X# — title**` scenario headers.
- Extract leading paragraph → "What this is", `What to try:` line → "What to try", `Expected:` line → "Expected".
- Emit a JSON manifest of 9 groups × 60 scenarios with `scenario_code` (A1…I9), `title`, formatted `description`, `order_index`.

### Phase 3 — Insert groups and scenarios
1. Insert 9 rows into `cycle_groups` with `order_index 0–8`, names matching the section titles, `description` set to a one-line summary of each section.
2. Bulk-insert 60 rows into `cycle_scenarios`, each with the standard 3-block markdown body, `has_steps = false`, `steps = NULL`.

### Phase 4 — Audit
Read-only verification:
- 1 cycle row with `cycle_code = 'CYC-012'`.
- 9 cycle_groups, ordered A–I.
- 60 cycle_scenarios; every description contains all three `**What this is:**`, `**What to try:**`, `**Expected:**` headers.
- Spot-check three scenarios (B1 — subject leakage P0, G2 — Take Action prefill, I6 — sub-tab subject scoping) for full content fidelity vs source doc.

## Technical notes

- All writes go through the **insert** tool (data-only, no schema change).
- The script will live in `/tmp/` (ephemeral) — not committed to the repo.
- After CYC-012 is verified, upload the Students doc and I'll repeat the same flow for CYC-013.

## Out of scope (this turn)

- CYC-013 (Students) — pending its source doc.
- No UI/code changes; pure data seeding for the existing Cycle Detail and Cycle Execution screens.

Approve and I'll execute Phases 1–4 in one go and report back with the audit numbers.