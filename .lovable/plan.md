## What I understood from your request

You want **CYC-011** created from scratch and seeded with the contents of `teacher-reports-chapters-qa.md`. This is the **first** of a three-cycle Teacher Reports series:

- **CYC-011 — Teacher Reports — Chapters QA** (this cycle, from the uploaded doc)
- CYC-012 — Teacher Reports — Exams QA (later, doc not yet uploaded)
- CYC-013 — Teacher Reports — Students QA (later, doc not yet uploaded)

Same pattern as CYC-010 (clean build, no user activity to preserve) — not the in-place edit pattern used for CYC-008/009. Cycle description = the rich preamble of the doc; each scenario formatted in the standard 3-block style (**What this is / What to try / Expected**).

## Source-document audit

Parsed 744 lines. Confirmed structure:

| Group | Section title | Scenarios |
|---|---|---|
| A | Chapters Tab Listing & Sorting | A1–A5 (5) |
| B | Chapter Overview Banner | B1–B4 (4) |
| C | Topic Heatmap | C1–C6 (6) |
| D | Student Buckets & PI Bucketing | D1–D7 (7) |
| E | Generate Homework — The 3-Step Page Flow | E1–E10 (10) |
| F | Chapter Practice History | F1–F5 (5) |
| G | Chapter Exam Breakdown & Cross-Navigation | G1–G5 (5) |
| H | Edge Cases, Curriculum Boundaries & Stability | H1–H5 (5) |
| I | Practice Session Detail Drill-In | I1–I7 (7) |

**Total: 9 groups, 54 scenarios.**

The doc preamble (lines 1–49) contains three rich sections that belong on the cycle description, not inside any scenario:
1. Intro paragraph (data-sensitivity warning).
2. **Threshold Reference table** (75/50/35 canonical vs 65/40/35 UI hard-codes vs Pass/fail bands) — critical for testers to know *before* filing color bugs.
3. **Before You Begin — Seed Your Data First** (institute admin seeding checklist).
4. **Highest-Risk Bugs to Hunt** (5 numbered P0/P1 patterns).

Section I uses a slightly different prose layout in the source (no explicit "What this is / What to try / Expected" headings on every scenario), so I'll normalize I1–I7 into the same 3-block format as A–H during parsing.

The footer note (line 684) referencing the sibling Exams/Students docs will be omitted from scenario text and used as a pointer when CYC-012/013 are seeded.

## Pre-flight check

- Project: `11111111-1111-1111-1111-111111111111` (foundational).
- Created by: `88326c88-1370-4d30-8eeb-e05941e74931` (admin, matches CYC-001 through CYC-010).
- Next cycle code is auto-generated by trigger `generate_cycle_code` → will produce `CYC-011`.
- ID convention: matching the CYC-008/009/010 pattern → `e1f2a3b4-c5d6-4e7f-8a90-111111111111`.

## Implementation plan

### Phase 1 — Insert the cycle row
Single insert into `test_cycles` with:
- `id = e1f2a3b4-c5d6-4e7f-8a90-111111111111`
- `name = 'Teacher Reports — Chapters QA'`
- `status = 'active'`, `priority = 'medium'`
- `project_id`, `created_by` as above
- `description` = ~5 KB markdown built from the doc preamble: opening paragraph + Threshold Reference table + Before You Begin checklist + Highest-Risk Bugs list. Trigger fills `cycle_code = 'CYC-011'` automatically.

### Phase 2 — Parse the markdown
Run a Python script (same approach as CYC-010) to:
- Split the doc by `### ` group headers (A–I).
- Within each group, split by `**X# — title**` scenario headers.
- For groups A–H, extract the explicit `What this is` / `What to try` / `Expected` blocks.
- For group I, normalize: first paragraph → "What this is", `What to try:` line → "What to try", `Expected:` line → "Expected".
- Strip the closing footer (line 684) so it doesn't leak into I1.
- Emit a JSON manifest of 9 groups × 54 scenarios with `scenario_code` (A1, A2, …, I7), `title`, formatted `description`, `order_index`.

### Phase 3 — Insert groups and scenarios
1. Insert 9 rows into `cycle_groups` with `order_index 0–8`, names matching the section titles, `description` set to a one-line summary of each section.
2. Bulk-insert 54 rows into `cycle_scenarios`, each with the standard 3-block markdown body, `has_steps = false`, `steps = NULL`.

### Phase 4 — Audit
Read-only verification:
- 1 cycle row with `cycle_code = 'CYC-011'`.
- 9 cycle_groups, ordered A–I.
- 54 cycle_scenarios; every description contains all three `**What this is:**`, `**What to try:**`, `**Expected:**` headers.
- Spot-check three scenarios (A1, E5, I5) for full content fidelity vs source doc.

## Technical notes

- All writes go through the **insert** tool (data-only, no schema change).
- No bug/comment/verdict data exists for CYC-011, so no preservation logic is needed.
- The script will live in `/tmp/` (ephemeral) — not committed to the repo.
- After CYC-011 is verified, you can upload the Exams and Students docs and I'll repeat the same flow for CYC-012 and CYC-013.

## Out of scope (this turn)

- CYC-012 (Exams) and CYC-013 (Students) — will be done when you upload their source docs.
- No UI/code changes; this is purely data seeding for the existing Cycle Detail and Cycle Execution screens.

Approve and I'll execute Phases 1–4 in one go and report back with the audit numbers.
